# METAMI Proof 仕様書

## 1. コンセプト

Markdown文書を校正するためのアプリ。原本を保持したまま、原本と同一の「修正文書」と、注釈を記録する「校正ログ（レイヤー）」を生成し、diff形式の2画面表示で赤ペン・青ペン・蛍光ペン・付箋・バルーンを使って複数人が校正作業を行う。個人情報・APIキー等の機密情報の漏洩を検知しつつ、確定した修正内容を通常のMarkdownとして出力する。

## 2. 想定ユーザー

- 編集者、ライター、校正者
- 複数人でレビューを行う制作チーム／広報・法務チェック担当

## 3. 主な機能一覧

### 3.1 ファイル管理

- Markdownファイルを読み込み
- **原本は変更せず保持**（イミュータブル）
- 読み込み時に、原本のコピーである「修正文書（右ペインの編集対象）」と、注釈を記録する「校正ログ（レイヤーデータ）」を生成する（詳細は6章・7章）
- 校正履歴のバージョン管理（誰が・いつ・何を校正したか）

### 3.2 個人情報・機密情報アラート

**個人情報**
- クレジットカード番号のパターン検知（Luhnアルゴリズムでの妥当性チェックを含む）
- メールアドレスの検知
- 必要に応じて電話番号、住所、マイナンバー等への拡張検知

**セキュリティ系（APIキー・認証情報）**
- APIキー・トークンのパターン検知（例：`sk-`, `AKIA`, `ghp_`, `xox[bp]-` などサービス固有のプレフィックス／Base64・Hex文字列の長さ・エントロピー判定）
- パスワード・秘密鍵らしき文字列（`-----BEGIN PRIVATE KEY-----` 等）の検知
- `.env` 的なキー＝値パターン（`API_KEY=`, `SECRET=`, `TOKEN=` 等）の検知
- 高エントロピー文字列検出による汎用シークレット検知（既知パターンに一致しない秘匿情報の取りこぼし対策）

**共通仕様**
- 検知箇所をハイライトし、警告バナー／アイコンで通知（種別ごとにアイコン・色を分けて識別しやすくする：個人情報＝黄、認証情報＝赤系など）
- 誤検知を除外するホワイトリスト機能
- **自動検知は100%を保証しない前提とし、最終判断は必ず人が行う**。検知は「見落とし防止のための一次チェック」と位置づけ、確定操作（反映／却下）はユーザーの明示的な判断に委ねる

### 3.3 校正ツール（マークアップ）

| ツール | 用途 | 表示イメージ（スタイル） |
|---|---|---|
| 赤ペン | 修正・削除指示 | 取り消し線＋字の挿入 |
| 青ペン | 補足・確認事項 | 下線＋コメント |
| 蛍光ペン | 注目箇所のハイライト | 半透明の背景色 |
| 付箋 | 段落・セクション単位のメモ | 文書横の余白に貼り付け表示 |
| バルーン | 特定箇所への吹き出しコメント | 該当箇所からの引き出し線＋コメント枠 |

- **色は固定仕様にしない**。「赤ペン＝赤、青ペン＝青」はデフォルトの目安に過ぎず、実際の色は**使用者の好み、または校正方針（社内ルール等）によって変更可能**とする（例：赤ペンだが誤字用は朱色、表現指摘用は臙脂色、など運用側で決めてよい）
- 色だけでは複数人・複数方針が混在した際に指示者を判別できないため、**すべての校正マークに校正者の「ニックネーム表記（ラベル）」を必ず併記**する（3.4参照）。色は視認性・好みのためのスタイル、識別はラベル表示が担う、という役割分担にする

### 3.4 複数人校正対応

- **識別方法**：色ではなく、各マークに校正者が自分で設定した**ニックネーム表記（ラベル）**を必ず表示することで識別する（色は使用者ごと・方針ごとに自由に設定できるため、識別の一次情報にはしない）
- **ラベルの仕様**：
  - ユーザーが自由に設定（例：「山田」を`Y`、`YM`、あるいは`(^^)`や`(・_・)`のような顔文字でも可）
  - 半角英数・記号（顔文字を組む括弧やアンダースコア等を含む）を許容し、**最大6文字程度**に制限してマーク周りが煩雑にならないようにする
  - **一意性は文書単位で保証する**：同じ文書内で既に他の校正者が使用しているラベルは、そもそも選択肢として選べない（入力時にリアルタイムで重複チェックし、使用済みのラベルは登録できない。警告して後から直させるのではなく、最初から選べないようにする）
- コメント・修正にユーザー名とタイムスタンプを付与
- フィルタ機能：特定の校正者のマークのみ表示／非表示（ラベルで絞り込み）
- 修正の承認・却下ワークフロー（オプション、レビュアー権限）

### 3.5 画面構成（2画面 diff 表示）

- **左ペイン**：原本＋校正レイヤー（詳細は7章）
- **右ペイン**：修正文書（初期状態は原本と同一、未反映マーカーで編集箇所を提示。詳細は7章）
- スクロール同期
- 上部ツールバー：ペン選択、校正者切り替え、表示フィルタ、左右入れ替えボタン
- 右サイドバー：付箋・コメント一覧、個人情報／セキュリティアラート一覧

### 3.6 出力

- すべての校正マークを「反映」または「却下」で確定（用語は7.2のマーカー状態と統一）
- 確定後、正式なMarkdownとして出力（校正記号やコメントは除去し本文のみ）
- 元のMarkdown記法（見出し、リスト、コードブロック等）を壊さず整合性を保つ
- 校正履歴はログとして別途書き出し可能（差分レポート or JSON）

## 4. 想定データフロー

1. ユーザーがMarkdownファイルをアップロード
2. システムが原本を保存し、修正文書（原本と同一の作業コピー）と校正ログ（レイヤーデータ）を生成
3. 個人情報・セキュリティ（APIキー等）スキャンを自動実行し、検知結果をアラート表示
4. 校正者が左ペイン（原本＋レイヤー）上で赤ペン等のツールを使い、修正・コメントを追加
5. 右ペイン（修正文書）の対応箇所に未反映マーカーが表示され、編集者が反映
6. 複数人が同時／非同時に校正（名前・イニシャルで識別）
7. 校正内容をレビューし、反映／却下を確定
8. 最終的なMarkdownファイルを出力、履歴を保存

## 5. 技術的な検討事項

- **Markdownパーサー**：remark / unified 系（ASTを操作すると書式を壊さず注釈を挿入しやすい）
- **Diffアルゴリズム**：diff-match-patch や Myers diff
- **共同編集方式**：個人アプリとしての最初の実装対象は**非同期レビュー**（保存→他の校正者が開き直すと最新の校正が見える方式）で十分。リアルタイム同時編集（CRDT／Yjs等）は当面実装予定なし。複数人が同時に使う要望が実際に出てきた段階で検討する
- **個人情報検知**：正規表現＋簡易バリデーション（クレカはLuhnアルゴリズム）、必要に応じ機械学習ベースのPIIモデルを併用
- **保存形式**：詳細は6.3の保存先一覧を正とする（`original.md` / `draft.md` / `draft.review.json` / `final.md`）
- **権限管理**：閲覧のみ／コメント可／編集可の3段階

## 6. 作業中データ（校正ログ）の保存

校正マーク・付箋・バルーンは本文に直接書き込まず、**原本に重ねる「注釈レイヤー」として別データで保持**する（非破壊編集）。保存形式は用途に応じて2階層で持つ。

### 6.1 正データ：構造化ログ（JSON／イベントソーシング）

1件の校正アクション＝1レコードとして**追記型ログ**で保存する。誰が・いつ・どこに・何をしたかを失わず、Undo・監査・複数人の同時作業のマージに強い形式。

```json
{
  "document_id": "doc_20260808_001",
  "original_file": "original.md",
  "original_hash": "sha256:1a2b3c...",
  "layers": [
    {
      "id": "anno_0001",
      "type": "red_pen",
      "reviewer": { "id": "u_yamada", "name": "山田", "label": "(^^)", "color": "#BE2A2C" },
      "created_at": "2026-08-08T10:22:00+09:00",
      "anchor": {
        "strategy": "text_context",
        "context_before": "本サービスへのお申込み",
        "target_text": "ありがとうございます",
        "context_after": "。登録確認のご連絡は",
        "ast_path": "root.children[1].children[2]"
      },
      "content": {
        "delete_text": "ありがとうございます",
        "insert_text": "誠にありがとうございます",
        "comment": null
      },
      "status": "pending"
      // "pending"（未反映）/ "in_progress"（反映中）/ "applied"（反映済み）/ "rejected"（却下）の4値。7.2のマーカー状態と対応
    }
  ],
  "events": [
    { "event": "annotation_added",   "layer_id": "anno_0001", "by": "山田", "at": "2026-08-08T10:22:00+09:00" },
    { "event": "annotation_applied", "layer_id": "anno_0001", "by": "鈴木", "at": "2026-08-08T11:05:00+09:00" }
  ]
}
```

**アンカリングの注意点**：行番号・文字オフセットだけで位置を保持すると、他の編集で本文がずれた際にマークの位置がずれる。特に大量の追加・削除が発生した場合は文脈の完全一致自体が失われるため、**確実性の高い方法から段階的にフォールバックする4段階の再アンカリング**を採用する。

1. **文脈完全一致**：`context_before` / `target_text` / `context_after` がそのまま本文中に見つかればそこにアンカー（最優先・最も確実）
2. **文脈あいまい一致**：完全一致が崩れていても、編集距離（Levenshtein等）や部分一致で近いテキストを探索し、一定の類似度スコアを超えたら暫定アンカーとする
3. **段落単位の推測**：文レベルで一致しない場合は、`ast_path` の段落（見出し・パラグラフのノード）が現存するかを確認し、該当段落の先頭に「この段落のどこかに校正指示あり」という粒度で仮アンカーする（ズレを段落内に閉じ込める）
4. **手動での再アンカリング**：上記いずれも失敗した場合はマークを「迷子（unanchored）」状態として一覧表示し、ユーザーが該当箇所をクリックして貼り直す。自動化に頼りきらず、必ず人が最終確定できる逃げ道を用意しておく

この4段階により、多少の書き換えでは自動追従し、大幅な書き換えでは無理に自動推測せず「迷子リスト」として可視化することで、誤った位置に指示が残ってしまうリスクを避ける。

**基本方針**：原本の改版によって生じるアンカーのズレは完全には防げないものとして受け入れる。過剰な精度（内容ハッシュによる段落指紋照合等の高度化）を最初から追求せず、まずは現状の文脈一致＋AST位置インデックスによる4段階フォールバックで「可能な範囲」まで対応し、それでもズレるものは迷子リストで人に委ねる。精度向上は運用してから必要に応じて検討する。

**色とラベルの役割分担**：`reviewer.color` は校正者個人の好み・方針で自由に設定できる表示色（識別の根拠にはしない）。`reviewer.label` を各マークの近くに常に表示することで、色が何であっても誰の指示かを一意に判別できるようにする。ラベルは半角英数・記号（顔文字含む）で最大6文字程度、ユーザーが自由に設定する。**一意性は同一文書単位で保証し、既に他の校正者が使用しているラベルはそもそも選択できない**（重複を許してから直させるのではなく、入力の時点でブロックする）。

### 6.2 可搬・可読形式：CriticMarkup（オプション）

構造化ログとは別に、注釈をMarkdown本文に直接埋め込む軽量記法として [CriticMarkup](http://criticmarkup.com/) 形式（`{++挿入++}` `{--削除--}` `{>>コメント<<}` `{==ハイライト==}`）でのエクスポートにも対応すると、他のエディタでの確認や差分共有がしやすくなる。ただしレビュアー情報・ステータス・スレッド返信などのメタデータは持てないため、**あくまで構造化ログのビュー（書き出し用）**という位置づけにする。

### 6.3 保存先の整理

| ファイル | 内容 | 可変性 |
|---|---|---|
| `original.md` | 原本 | 不変 |
| `draft.md` | 修正文書（右ペインの編集対象、初期状態は原本と同一） | 校正者が編集 |
| `draft.review.json` | 校正ログ（正データ：レイヤー・イベント） | 追記のみ |
| `draft.critic.md`（任意） | CriticMarkup書き出し | ログから再生成 |
| `final.md` | 確定後の出力 | 確定時に生成 |

## 7. 画面構成の改訂：レイヤー構造と左右入れ替え

### 7.1 レイヤー構造（左ペイン＝原本）

- 左ペインは**原本テキスト＋その上に重ねた校正レイヤー**を表示する（原本データ自体は書き換えない）
- 赤ペン・青ペン・蛍光ペン・付箋・バルーンは、すべて原本の上のレイヤーとして描画される「校正指示」という位置づけ
- レイヤーの表示ON/OFF、校正者ごとの表示切り替えが可能（既存のサイドバー「校正者フィルタ」と連動）

### 7.2 修正文書（右ペイン）の初期状態と反映導線

- 右ペインの文書は**初期状態では原本と完全に同一**（校正レイヤーは含まない、まっさらな編集対象）
- 左ペインに校正マーク（レイヤー）が追加されると、右ペインの対応箇所に**未反映マーカー**（点滅ドット・波線など）が自動的に表示され、「ここを直してください」と視覚的に促す
- マーカーの状態遷移：
  - 🔴 **未反映**（点滅）：左に指示はあるが右にまだ反映されていない
  - 🟡 **反映中**：編集者が右側で該当箇所を編集し始めた状態
  - ✅ **反映済み**：内容が反映され、レイヤー側のステータスが `applied` に更新（点滅は止まりチェックマークに変化）
  - ⬜ **却下**：指示を見送った場合。マーカーはグレーアウトして残す（見送った経緯を残す）
- 点滅などのアニメーションは `prefers-reduced-motion` を尊重し、視覚的な負担を減らすオプション（点滅→静的アイコンのみ）も用意する

### 7.3 左右入れ替え機能

- ヘッダーに⇄（入れ替え）ボタンを設置し、原本ペインと修正文書ペインの表示位置をワンクリックで反転できる
- 用途：普段は「原本が左・修正文書が右」で作業するが、修正文書を主軸にレビューしたい場面では左右を入れ替えて「修正文書を左」に固定する、といった使い方を想定
- 入れ替えてもレイヤー表示・未反映マーカーの対応関係（どちらが原本でどちらが修正文書か）は色・ラベルで明示し、混同を防ぐ

## 8. 原本が外部で変更された場合の対応

校正レイヤーは「原本＋そこに重ねた注釈」という前提で成立しているため、**原本そのものがアプリ外で書き換えられるケース**を明示的に扱う必要がある。

### 8.1 変更の検知

- レビューセッション開始時に原本のハッシュ（`original_hash`）を記録
- ファイルを開くたびにハッシュを再計算し、記録済みハッシュと比較
- 比較結果によって以下の3パターンに分岐する

| パターン | 判定 | 挙動 |
|---|---|---|
| ハッシュ一致 | 前回と同一の原本 | 通常どおりレイヤーを読み込んで継続 |
| ハッシュ不一致（同一ファイル扱い） | 原本が外部で書き換えられた | **8.2 の対応フロー**へ |
| 未知のファイル | 過去のセッションと紐付かない | 新規文書として開始 |

### 8.2 原本がずれた場合の対応フロー

1. **アラート表示**：「原本が校正開始時から変更されています」と明示し、無自覚に古い指示のまま作業を進めないようにする
2. **差分プレビュー**：変更前（レイヤー作成時点の原本）と変更後（現在のファイル）の差分を見せ、変更が軽微（誤字修正程度）か大幅（構成変更）かをユーザー自身が判断できるようにする
3. **ユーザーが選択**：
   - **新規文書として開始**：既存の校正レイヤーは新しい原本には適用せず、まっさらな状態から校正を始める。**既に手動編集していた`draft.md`（修正文書）も同様に、そのまま新しい原本に引き継がず、レイヤーと合わせて履歴として保管した上で、新しい`draft.md`は新原本のコピーから作り直す**（過去の編集内容は失わないが、自動マージはしない。マージが必要なら過去の履歴を見ながら手動で反映してもらう）
   - **既存の校正データを新しい原本にマッチングする（再アンカリング）**：7章で定義した**4段階フォールバック**（文脈完全一致→あいまい一致→段落推測→手動）を使って、既存の各レイヤーを新しい原本に対して再アンカリングする。成功したものはそのまま位置を引き継ぎ、失敗したものは「迷子」リストに上げてユーザーに手動対応を促す
- 差分の規模が小さい場合は「マッチングを推奨」、大きく書き換わっている場合は「新規開始を推奨」といった**目安の提示**はするが、最終判断は必ずユーザーが行う（自動でどちらかに決め切らない）

### 8.3 校正済み文書（final.md）を再度アプリで開いた場合

- 確定済みの出力は、校正マークがすべて解消（`applied` / `rejected`）された状態のテキストであり、原理的に引き継ぐべき未解決レイヤーが存在しない
- そのため、**基本挙動は「新規文書として開始」**とする（8.2のような複雑なマッチングは不要）
- ただし完全に無関係な文書としては扱わず、メタデータとして「どの文書から派生したか（`derived_from: { document_id, final_hash }`）」を記録し、**文書の来歴（バージョン履歴）**として遡れるようにしておく。これにより「このv2文書はv1のどの校正を経て確定したか」を後から追跡できる
- 実装はシンプルに倒す：`final.md`を書き出す際、本文とは別にMarkdownのfrontmatter（`---`で囲むメタデータ）へ`document_id`・`derived_from`を埋め込んでおく。次にその`final.md`を開いた際、アプリはfrontmatterを読むだけで来歴を判定でき、別途DBで管理する必要がない

## 10. 開発ロードマップ（フェーズ分け）

第一稿の範囲はPhase1〜2を目安とし、それ以降は必要になった時点で拡張する。

| Phase | 内容 |
|---|---|
| **Phase1（MVP）** | ファイル読込／原本保持／2画面表示（原本＋レイヤー、修正文書）／赤ペン等の注釈UI（単独ユーザー）／JSON保存／最終md出力 |
| **Phase2** | 複数人・非同期対応（名前・イニシャル識別、フィルタ）／個人情報・セキュリティアラート（正規表現＋Luhn等）／原本外部変更の検知・再アンカリング |
| **Phase3（v1以降）** | **次の校正者へのデータ引き継ぎ用エクスポート機能**（校正ログ・未反映マーカー・迷子リストを含めた引き継ぎパッケージの書き出し／取り込み）／CriticMarkup書き出し |
| **Phase4（要望が出た場合）** | リアルタイム共同編集（CRDT導入） |

Phase3の引き継ぎエクスポートは、`draft.review.json`（校正ログ）と`draft.md`（修正文書）をセットで受け渡す形を想定。受け取った側は原本ハッシュを照合し、8章の変更検知フローと同じ仕組みでそのまま校正を再開できるようにする。

## 11. 今後の検討ポイント

- リアルタイム同時編集 vs 非同期レビューのどちらを優先するか（当面は非同期のみ実装）
- 個人情報・認証情報アラートの誤検知率とホワイトリスト運用ルール
- モバイル対応の要否（付箋・バルーンのUIは狭い画面での工夫が必要）
- エクスポート形式（Markdownのみか、diffレポートも含めるか）
- アンカリング方式（文脈テキスト／AST位置）の精度検証と、本文が大きく書き換わった場合のマーク追従ロジック
- 「反映済み」判定を自動化するか（編集後のテキスト類似度で自動検知）、editorの手動操作に委ねるか
- 原本外部変更時の「差分プレビュー」の見せ方（diffビューをどこまで詳細に出すか）
- 再アンカリング失敗時の「迷子」マーク一覧UIの設計
